{
  "installCommand": "npm install --prefix sdk && cd sdk && npx tsup && cd .. && npm install --prefix frontend",
  "buildCommand": "cd frontend && npx next build",
  "outputDirectory": "frontend/.next",
  "framework": "nextjs"
}

{
  "installCommand": "cd sdk && npm install && npx tsup && cd ../frontend && npm install",
  "buildCommand": "cd frontend && npx next build",
  "outputDirectory": "frontend/.next",
  "framework": "nextjs"
}
